[project]
name = "teleco-daisy"
version = "0.1.12"
description = ""
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2",
    "requests>=2.28",
]

[dependency-groups]
dev = [
    "icecream>=2.1.5",
    "ruff>=0.12.3",
]
